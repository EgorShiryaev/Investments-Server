{
  "name": "investment_portfolio_server",
  "version": "1.0.0",
  "description": "Server for mobile app \"Investment Portfolio\"",
  "main": "dist/src/app.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "startServer": "node dist/src/app.js",
    "buildAndRunServer": "npm run build && npm run startServer"
  },
  "author": "Egor Shiryaev",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "http": "0.0.1-security",
    "sqlite3": "^5.1.1",
    "ws": "^8.9.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.3",
    "@types/express": "^4.17.14"
  }
}
